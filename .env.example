# Copy to .env.local to override.

# Where the contact form posts.
# Leave unset to use Netlify Forms (the form markup already carries the attributes
# Netlify's build step looks for). Set this to post JSON to a CRM intake endpoint,
# serverless function, or a service like Formspree instead.
# VITE_FORM_ENDPOINT=https://example.com/api/contact
