# SPA fallback. Without this, a direct hit or refresh on /projects/aspen returns 404
# because no such file exists on disk — routing happens in the browser.
# Netlify and Cloudflare Pages both read this file.
/*  /index.html  200
